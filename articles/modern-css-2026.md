---
title: "CSSは2015年じゃない——2026年版モダンCSSの実践ガイド"
emoji: "🎨"
type: "tech"
topics: ["css", "frontend", "webdev"]
published: true
---

## はじめに — CSS、この2〜3年で10年分進化した

「CSSって、floatでレイアウト組んで、clearfixして、ベンダープレフィックスつけて……」

もしあなたのCSS知識が2015年で止まっているなら、今のCSSを見たら別の言語だと思うかもしれない。冗談じゃなく、**2023〜2025年の進化は、それ以前の10年分に匹敵する**。

Flexbox と Grid でレイアウト革命が起きたのは記憶に新しいが、それは序章に過ぎなかった。コンテナクエリ、親セレクタ、ネイティブネスト、カスケードレイヤー、スクロール連動アニメーション、View Transitions——かつて「CSSだけでは無理」と言われていた機能が、次々とブラウザに実装された。

しかも、これらの多くは**すでにブラウザ対応率90%超**だ。「まだ早い」ではなく、「もう使える」フェーズに入っている。

この記事では、2026年の今こそ押さえておくべき**9つのモダンCSS機能**を、コード例・ブラウザ対応率・実践的なユースケースとともに解説する。Sassに依存していた構文、JSで無理やり実装していたUI、ハック的なワークアラウンド——それらの多くが、もう純粋なCSSで実現できる。

読み終わる頃には、「CSS、めちゃくちゃ進化してたんだな」と感じてもらえるはずだ。

---

## 1. `@container` コンテナクエリ — コンポーネント単位のレスポンシブ

**ブラウザ対応率：95.9%** ✅

### これまでの課題

メディアクエリは**ビューポート幅**に基づく。だが現実のUIでは、同じコンポーネントがサイドバーにもメインエリアにも配置される。ビューポートは同じなのに、コンポーネントが使える幅はまったく違う。これがメディアクエリの限界だった。

### コンテナクエリで解決

コンテナクエリは、**親コンテナの幅**に基づいてスタイルを切り替える。コンポーネントが「自分がどこに置かれているか」を知り、自律的にレスポンシブに振る舞える。

```css
/* 親をコンテナとして宣言 */
.card-wrapper {
  container-type: inline-size;
  container-name: card;
}

/* コンテナの幅が400px以上なら横並びに */
@container card (min-inline-size: 400px) {
  .card {
    display: grid;
    grid-template-columns: 200px 1fr;
    gap: 1.5rem;
  }

  .card__title {
    font-size: 1.5rem;
  }
}

/* コンテナの幅が400px未満なら縦積み */
@container card (max-inline-size: 399px) {
  .card {
    display: flex;
    flex-direction: column;
  }

  .card__title {
    font-size: 1.125rem;
  }
}
```

### 実践ポイント

- `container-type: inline-size` は**インライン方向（通常は横幅）** のみをコンテナクエリの対象にする。ほとんどのケースではこれで十分
- `container-name` を指定すると、どのコンテナに対するクエリかを明示できる。ネストが深い場合に重要
- デザインシステムとの相性が抜群。**コンポーネントが配置場所に依存しない**ため、再利用性が格段に上がる

### メディアクエリとの使い分け

| 判断基準 | メディアクエリ | コンテナクエリ |
|---|---|---|
| 対象 | ビューポート | 親コンテナ |
| 用途 | ページ全体のレイアウト | コンポーネント内部 |
| 適した場面 | ナビゲーション切替 | カード・ウィジェット |

メディアクエリが不要になったわけではない。**ページレベルのレイアウトはメディアクエリ、コンポーネントレベルはコンテナクエリ**という使い分けがベストプラクティスだ。

---

## 2. `:has()` セレクタ — 親セレクタ革命

**ブラウザ対応率：95.4%** ✅

### CSSに「親セレクタ」がなかった時代

CSSセレクタは常に「上から下」だった。`.parent .child` は書けるが、「`.child` を持つ `.parent`」は書けなかった。この制限は長年「CSSの最大の弱点」と言われ、JSで補うしかなかった。

`:has()` はこの20年来の課題を解決する。

### 基本的な使い方

```css
/* 画像を含むカードだけ、パディングを変える */
.card:has(img) {
  padding: 0;
}

/* チェックされたチェックボックスを持つラベルをハイライト */
label:has(input:checked) {
  background-color: oklch(0.9 0.05 140);
  border-color: oklch(0.6 0.15 140);
}

/* 空のリストには「データなし」メッセージを表示 */
.list:has(> :first-child) ~ .empty-message {
  display: none;
}

/* フォームにinvalidなフィールドがあればボタンを薄くする */
form:has(:user-invalid) button[type="submit"] {
  opacity: 0.5;
  pointer-events: none;
}
```

### 応用：ページ全体の制御

```css
/* ダークモード切替をCSSだけで */
html:has(#dark-mode-toggle:checked) {
  color-scheme: dark;
  --bg: oklch(0.2 0.02 260);
  --text: oklch(0.9 0.01 260);
}

/* モーダルが開いているときbodyをスクロールロック */
body:has(dialog[open]) {
  overflow: hidden;
}

/* サイドバーが展開されているときメインコンテンツの幅を変える */
.layout:has(.sidebar[aria-expanded="true"]) .main {
  grid-template-columns: 280px 1fr;
}
```

### パフォーマンスに関する注意

`:has()` は強力だが、ブラウザは**逆方向のマッチング**を行う必要がある。以下のガイドラインを守ろう：

- セレクタはできるだけ**限定的**に書く（`*:has(...)` は避ける）
- `:has()` のネストは浅く保つ
- 大量のDOM要素に対して使う場合はパフォーマンスを検証する

---

## 3. CSS Nesting — Sassなしでネストが書ける

**ブラウザ対応率：91.2%** ✅

### Sassの最大の魅力がネイティブに

CSSプリプロセッサを使う理由の第一位は「ネスト」だった。コンポーネントのスタイルを構造的に書けるネストは、可読性と保守性を大幅に向上させる。

2024年、ネストがすべてのモダンブラウザでネイティブサポートされた。

```css
/* ネイティブCSSネスト */
.card {
  background: var(--surface);
  border-radius: 12px;
  padding: 1.5rem;

  /* 子要素 */
  & .title {
    font-size: 1.25rem;
    font-weight: 700;
  }

  & .description {
    color: var(--text-muted);
    line-height: 1.7;
  }

  /* 擬似クラス */
  &:hover {
    box-shadow: 0 4px 12px oklch(0 0 0 / 0.1);
  }

  /* メディアクエリもネスト可能 */
  @media (width >= 768px) {
    display: grid;
    grid-template-columns: 1fr 2fr;
  }

  /* コンテナクエリもネスト可能 */
  @container card (inline-size >= 400px) {
    flex-direction: row;
  }
}
```

### Sassとの違い

```css
/* Sass記法（&なしでも子セレクタになる） */
.card {
  .title { ... }     /* Sassでは .card .title */
}

/* ネイティブCSS（2024年以降、&なしでも要素セレクタ以外はOK） */
.card {
  .title { ... }     /* .card .title として解釈される */
  h2 { ... }         /* これも動く（以前は & h2 が必要だった） */
}
```

初期の仕様では要素セレクタ（`h2`, `p` など）のネストに `&` が必須だったが、**2024年のアップデートで緩和**された。現在はほぼSassと同じ感覚で書ける。

### ネストの深さに注意

ネストは便利だが、**3階層以上は避ける**のが鉄則だ。

```css
/* ❌ 深すぎるネスト — 詳細度が高くなりすぎる */
.page {
  .section {
    .card {
      .title {
        .icon { ... }
      }
    }
  }
}

/* ✅ フラットに保つ */
.card {
  & .title { ... }
  & .icon { ... }

  &--featured {
    border: 2px solid var(--accent);
  }
}
```

---

## 4. `@layer` カスケードレイヤー — 詳細度戦争の終結

**ブラウザ対応率：95.6%** ✅

### 詳細度（Specificity）という悪夢

CSSを書いていて「なぜこのスタイルが当たらないんだ」と `!important` を追加した経験はないだろうか。サードパーティCSS、リセットCSS、コンポーネントCSS——それらの詳細度が衝突し、意図しないスタイルの上書きが起きる。これが**詳細度戦争**だ。

`@layer` は、この問題をアーキテクチャレベルで解決する。

### 基本的な使い方

```css
/* レイヤーの順序を宣言（後に書いたものが優先） */
@layer reset, base, components, utilities;

/* リセットCSS — 最も低い優先度 */
@layer reset {
  *,
  *::before,
  *::after {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
  }
}

/* ベーススタイル */
@layer base {
  body {
    font-family: "Inter", system-ui, sans-serif;
    line-height: 1.7;
    color: var(--text);
  }

  a {
    color: var(--link);
    text-decoration: none;
  }
}

/* コンポーネント */
@layer components {
  .btn {
    display: inline-flex;
    align-items: center;
    padding: 0.75rem 1.5rem;
    border-radius: 8px;
    font-weight: 600;
    background: var(--primary);
    color: white;
  }
}

/* ユーティリティ — 最も高い優先度 */
@layer utilities {
  .sr-only {
    position: absolute;
    width: 1px;
    height: 1px;
    overflow: hidden;
    clip: rect(0 0 0 0);
  }

  .text-center {
    text-align: center;
  }
}
```

### なぜ革命的なのか

`@layer` の核心は、**詳細度よりもレイヤー順が優先される**ことだ。

```css
@layer base, components;

@layer base {
  /* 詳細度：0-1-1 */
  section .title {
    color: black;
  }
}

@layer components {
  /* 詳細度：0-1-0（baseより低い！） */
  .title {
    color: blue;
  }
}

/* → .title は blue になる。componentsレイヤーがbaseより後だから */
```

詳細度が低くても、レイヤー順が後なら勝つ。つまり、**セレクタの詳細度をハックする必要がなくなる**。

### サードパーティCSSとの統合

```css
/* 外部CSSを低優先レイヤーに押し込む */
@import url("third-party.css") layer(vendor);

@layer vendor, app;

/* 自分のスタイルは常にvendorより優先される */
@layer app {
  .btn {
    /* third-party.css の .btn を !important なしで上書きできる */
    background: var(--my-primary);
  }
}
```

---

## 5. `oklch()` / `color-mix()` — 知覚ベースのカラーシステム

**ブラウザ対応率：約95%** ✅

### HSLの問題

HSLは人間に分かりやすい色モデルとして広まったが、大きな欠陥がある。**同じLightness値でも、色相によって知覚的な明るさが異なる**のだ。

```css
/* HSLでは同じ lightness: 50% でも… */
.yellow { background: hsl(60, 100%, 50%); }   /* 非常に明るく見える */
.blue   { background: hsl(240, 100%, 50%); }  /* 暗く見える */
```

黄色と青で同じ `50%` なのに、見た目の明るさがまったく違う。これはHSLが**知覚的に均一ではない**からだ。

### oklch() で解決

`oklch()` は知覚的に均一な色空間で、同じLightness値は**本当に同じ明るさに見える**。

```css
:root {
  /* oklch(Lightness Chroma Hue) */
  --primary:   oklch(0.55 0.20 260);  /* 鮮やかなブルー */
  --secondary: oklch(0.55 0.20 330);  /* 同じ明るさのパープル */
  --success:   oklch(0.55 0.20 145);  /* 同じ明るさのグリーン */
  --warning:   oklch(0.55 0.20 85);   /* 同じ明るさのオレンジ */
  --danger:    oklch(0.55 0.20 25);   /* 同じ明るさのレッド */
}
```

すべて `Lightness: 0.55` なので、**カラーパレット全体で視覚的な一貫性が保たれる**。

### color-mix() でバリエーション生成

```css
:root {
  --primary: oklch(0.55 0.20 260);

  /* プライマリカラーのバリエーションを自動生成 */
  --primary-light:  color-mix(in oklch, var(--primary) 40%, white);
  --primary-dark:   color-mix(in oklch, var(--primary) 70%, black);
  --primary-subtle: color-mix(in oklch, var(--primary) 15%, transparent);
  --primary-hover:  color-mix(in oklch, var(--primary) 85%, white);
}

/* ダークモード対応もシンプルに */
@media (prefers-color-scheme: dark) {
  :root {
    --surface:    oklch(0.2 0.02 260);
    --text:       oklch(0.9 0.01 260);
    --border:     color-mix(in oklch, var(--text) 15%, transparent);
  }
}
```

### 実践：テーマカラーの一括管理

```css
/* Hue だけ変えれば、ブランドカラー全体が一貫して変わる */
:root {
  --hue: 260;
  --brand:        oklch(0.55 0.20 var(--hue));
  --brand-light:  oklch(0.75 0.12 var(--hue));
  --brand-dark:   oklch(0.35 0.15 var(--hue));
  --brand-bg:     oklch(0.97 0.02 var(--hue));
}

/* テーマ切替が Hue の変更だけで完結 */
[data-theme="green"] { --hue: 145; }
[data-theme="purple"] { --hue: 310; }
[data-theme="orange"] { --hue: 55; }
```

---

## 6. `@starting-style` + `@property` — display:none からのアニメーション

**ブラウザ対応率：91.6%** ✅

### display:none からのトランジションという長年の課題

CSSで最もフラストレーションが溜まる制限の一つ——`display: none` から `display: block` への**トランジションができない**。要素が突然現れ、フェードインもスライドインもできない。この問題のためだけにJSライブラリを導入していた人も多いはずだ。

`@starting-style` と `allow-discrete` が、この問題を解決する。

### @starting-style の基本

```css
/* ダイアログのフェードイン・フェードアウト */
dialog {
  opacity: 0;
  transform: translateY(-20px);
  transition:
    opacity 0.3s ease,
    transform 0.3s ease,
    overlay 0.3s ease allow-discrete,
    display 0.3s ease allow-discrete;

  /* 要素が表示される「前」のスタイルを定義 */
  @starting-style {
    opacity: 0;
    transform: translateY(-20px);
  }
}

/* 開いた状態 */
dialog[open] {
  opacity: 1;
  transform: translateY(0);
}
```

`@starting-style` は、要素がDOMに挿入される（または `display: none` から表示される）**直前のスタイル**を定義する。ブラウザはこの初期状態から目標状態へトランジションを実行する。

### @property でカスタムプロパティをアニメーション

通常、CSS変数（カスタムプロパティ）はアニメーションできない。`@property` を使えば型を定義でき、補間が可能になる。

```css
/* カスタムプロパティの型を定義 */
@property --gradient-angle {
  syntax: "<angle>";
  initial-value: 0deg;
  inherits: false;
}

@property --gradient-color {
  syntax: "<color>";
  initial-value: oklch(0.55 0.20 260);
  inherits: false;
}

.gradient-border {
  --gradient-angle: 0deg;
  --gradient-color: oklch(0.55 0.20 260);

  border: 3px solid transparent;
  background:
    linear-gradient(var(--surface), var(--surface)) padding-box,
    conic-gradient(
      from var(--gradient-angle),
      var(--gradient-color),
      oklch(0.65 0.25 330),
      var(--gradient-color)
    ) border-box;

  transition: --gradient-angle 0.8s ease, --gradient-color 0.5s ease;

  &:hover {
    --gradient-angle: 180deg;
    --gradient-color: oklch(0.65 0.25 330);
  }
}
```

### 実践：トースト通知

```css
.toast {
  position: fixed;
  bottom: 2rem;
  right: 2rem;
  background: var(--surface);
  border-radius: 12px;
  padding: 1rem 1.5rem;
  box-shadow: 0 8px 24px oklch(0 0 0 / 0.15);

  /* 閉じるアニメーション */
  opacity: 0;
  translate: 0 100%;
  transition:
    opacity 0.4s ease,
    translate 0.4s ease,
    display 0.4s ease allow-discrete;

  /* 開くときの初期状態 */
  @starting-style {
    opacity: 0;
    translate: 0 100%;
  }
}

.toast[data-visible="true"] {
  opacity: 1;
  translate: 0 0;
}
```

JSで `data-visible` 属性を切り替えるだけで、スムーズなスライドイン・フェードアウトが実現できる。アニメーションライブラリは不要だ。

---

## 7. Scroll-driven Animations — JSなしのスクロール連動

**ブラウザ対応率：77%** ⚠️（Safari未対応）

### IntersectionObserver も GSAP も不要に

スクロールに連動するアニメーションは、これまでJavaScript（IntersectionObserver、GSAP ScrollTrigger、Framer Motionなど）の領域だった。CSS Scroll-driven Animations は、これを**純粋なCSSだけ**で実現する。

### 基本：スクロール進捗バー

```css
/* ページのスクロール進捗を示すバー */
.progress-bar {
  position: fixed;
  top: 0;
  left: 0;
  height: 3px;
  background: var(--primary);
  transform-origin: left;
  /* スクロール量に応じて0%→100%にスケール */
  animation: grow-progress linear;
  animation-timeline: scroll();
}

@keyframes grow-progress {
  from { transform: scaleX(0); }
  to   { transform: scaleX(1); }
}
```

`animation-timeline: scroll()` と書くだけで、通常の時間ベースアニメーションが**スクロール位置ベース**に変わる。

### 要素が画面に入ったらアニメーション

```css
/* 画面内に入ったらフェードイン */
.reveal {
  opacity: 0;
  transform: translateY(30px);

  animation: reveal-in linear both;
  animation-timeline: view();
  /* 要素の上端がビューポートの下25%に来たらスタート */
  animation-range: entry 25% cover 50%;
}

@keyframes reveal-in {
  to {
    opacity: 1;
    transform: translateY(0);
  }
}
```

`animation-timeline: view()` は、**要素自体のビューポート内での位置**に基づくタイムラインだ。`animation-range` で開始・終了タイミングを細かく制御できる。

### パララックス効果

```css
.parallax-image {
  animation: parallax linear;
  animation-timeline: view();
}

@keyframes parallax {
  from { transform: translateY(-50px); }
  to   { transform: translateY(50px); }
}
```

### Safari未対応への対処

2026年2月時点で、SafariはScroll-driven Animationsを**サポートしていない**。プログレッシブエンハンスメントで対応しよう：

```css
/* Scroll-driven Animations非対応ブラウザでは静的表示 */
.reveal {
  opacity: 1;
  transform: none;
}

/* 対応ブラウザでのみアニメーション */
@supports (animation-timeline: view()) {
  .reveal {
    opacity: 0;
    transform: translateY(30px);
    animation: reveal-in linear both;
    animation-timeline: view();
    animation-range: entry 25% cover 50%;
  }
}
```

`@supports` でフィーチャーディテクションすれば、非対応ブラウザでもコンテンツは正常に表示される。

---

## 8. Popover API + Anchor Positioning — ネイティブなポップオーバー

**ブラウザ対応率：Popover 93% ✅ / Anchor Positioning 77% ⚠️**

### JS製ツールチップ・ドロップダウンの終焉

ツールチップ、ドロップダウンメニュー、ポップオーバー——これらはこれまで、位置計算・フォーカス管理・Escape キー処理・背景クリックでの閉じ処理をすべてJSで実装する必要があった。Floating UI や Popper.js のようなライブラリが事実上の標準だった。

Popover API は、これらの振る舞いを**ブラウザネイティブ**で提供する。

### Popover API の基本

```html
<!-- HTML だけで動作する！JSなし -->
<button popovertarget="menu">メニューを開く</button>

<div id="menu" popover>
  <nav>
    <a href="/dashboard">ダッシュボード</a>
    <a href="/settings">設定</a>
    <a href="/profile">プロフィール</a>
  </nav>
</div>
```

```css
/* Popover のスタイリング */
[popover] {
  margin: 0;
  padding: 1rem;
  border: 1px solid var(--border);
  border-radius: 12px;
  box-shadow: 0 8px 24px oklch(0 0 0 / 0.12);
  background: var(--surface);

  /* 開閉アニメーション */
  opacity: 0;
  transform: scale(0.95) translateY(-8px);
  transition:
    opacity 0.2s ease,
    transform 0.2s ease,
    overlay 0.2s ease allow-discrete,
    display 0.2s ease allow-discrete;

  @starting-style {
    opacity: 0;
    transform: scale(0.95) translateY(-8px);
  }
}

[popover]:popover-open {
  opacity: 1;
  transform: scale(1) translateY(0);
}

/* 背景のオーバーレイ */
[popover]::backdrop {
  background: oklch(0 0 0 / 0.2);
  transition: background 0.2s ease;
}
```

`popover` 属性を追加するだけで、以下が**自動的に**提供される：

- トップレイヤーへの昇格（z-index戦争とは無縁）
- Escapeキーで閉じる
- 外部クリックで閉じる（light dismiss）
- フォーカストラップ
- `::backdrop` 擬似要素

### Anchor Positioning

Anchor Positioning を使えば、ポップオーバーを**トリガー要素に対して相対的に配置**できる。

```css
/* トリガーをアンカーとして定義 */
.trigger {
  anchor-name: --trigger;
}

/* ポップオーバーをアンカーに対して配置 */
[popover] {
  position: fixed;
  position-anchor: --trigger;

  /* トリガーの下に配置 */
  inset-area: bottom span-right;
  margin-top: 8px;

  /* 画面端で自動的に位置を調整 */
  position-try-fallbacks: flip-block, flip-inline;
}
```

`position-try-fallbacks` は、ポップオーバーが画面外にはみ出す場合に**自動的に反対側に配置し直す**機能だ。Floating UIの `flip` ミドルウェアがCSSネイティブになったと考えてよい。

:::message
Anchor Positioning は2026年2月時点でChromeとEdgeが対応済み、Safari/Firefoxは開発中。プロダクション利用にはフォールバックを用意しよう。
:::

---

## 9. View Transitions — SPAのような画面遷移

**ブラウザ対応率：89.4%** ✅

### ページ遷移のぎこちなさを解消

従来のMPA（マルチページアプリケーション）では、ページ遷移時に画面が一瞬白くなる。SPAフレームワーク（React、Next.js、Nuxt）ではルーターで制御できたが、MPAでは不可能だった。

View Transitions API は、**MPA・SPA問わず**スムーズな画面遷移を実現する。

### MPA での View Transitions（CSS だけ！）

```css
/* ページ全体のクロスフェード */
@view-transition {
  navigation: auto;
}

/* デフォルトのフェードアニメーション */
::view-transition-old(root) {
  animation: fade-out 0.3s ease;
}

::view-transition-new(root) {
  animation: fade-in 0.3s ease;
}

@keyframes fade-out {
  to { opacity: 0; }
}

@keyframes fade-in {
  from { opacity: 0; }
}
```

たったこれだけで、MPAのページ遷移がクロスフェードになる。

### 要素単位の遷移（Shared Element Transitions）

```css
/* 一覧ページのカード画像 */
.card__image {
  view-transition-name: hero-image;
}

/* 詳細ページのヒーロー画像 */
.detail__hero {
  view-transition-name: hero-image;
}

/* カスタムアニメーション */
::view-transition-old(hero-image) {
  animation: scale-down 0.4s cubic-bezier(0.4, 0, 0.2, 1);
}

::view-transition-new(hero-image) {
  animation: scale-up 0.4s cubic-bezier(0.4, 0, 0.2, 1);
}

@keyframes scale-down {
  to {
    transform: scale(0.8);
    opacity: 0;
  }
}

@keyframes scale-up {
  from {
    transform: scale(0.8);
    opacity: 0;
  }
}
```

同じ `view-transition-name` を持つ要素同士がページ間で**スムーズにモーフィング**する。一覧→詳細の画像遷移、タブ切替時のコンテンツスライドなど、ネイティブアプリのような体験がCSSだけで実現できる。

### SPA での View Transitions

```javascript
// JSからのトリガー（React / Vue / Vanilla）
document.startViewTransition(() => {
  // DOMの更新処理
  updateContent(newPage);
});
```

```css
/* SPA用のスライドアニメーション */
::view-transition-old(content) {
  animation: slide-out-left 0.3s ease;
}

::view-transition-new(content) {
  animation: slide-in-right 0.3s ease;
}

@keyframes slide-out-left {
  to { transform: translateX(-100%); opacity: 0; }
}

@keyframes slide-in-right {
  from { transform: translateX(100%); opacity: 0; }
}
```

### motion 設定の尊重

```css
@media (prefers-reduced-motion: reduce) {
  ::view-transition-group(*),
  ::view-transition-old(*),
  ::view-transition-new(*) {
    animation: none !important;
  }
}
```

アクセシビリティの観点から、`prefers-reduced-motion` でアニメーションを無効化するのを忘れずに。

---

## ブラウザ対応まとめ

2026年2月時点の各機能のブラウザ対応状況を一覧にまとめる。

| 機能 | Chrome | Firefox | Safari | 対応率 | 本番利用 |
|---|---|---|---|---|---|
| `@container` | ✅ 105+ | ✅ 110+ | ✅ 16+ | 95.9% | ✅ 安心 |
| `:has()` | ✅ 105+ | ✅ 121+ | ✅ 15.4+ | 95.4% | ✅ 安心 |
| CSS Nesting | ✅ 120+ | ✅ 117+ | ✅ 17.2+ | 91.2% | ✅ 安心 |
| `@layer` | ✅ 99+ | ✅ 97+ | ✅ 15.4+ | 95.6% | ✅ 安心 |
| `oklch()` / `color-mix()` | ✅ 111+ | ✅ 113+ | ✅ 16.2+ | ~95% | ✅ 安心 |
| `@starting-style` | ✅ 117+ | ✅ 129+ | ✅ 17.5+ | 91.6% | ✅ 安心 |
| Scroll-driven Anim. | ✅ 115+ | ✅ | ❌ 未対応 | 77% | ⚠️ PE推奨 |
| Popover API | ✅ 114+ | ✅ 125+ | ✅ 17+ | 93% | ✅ 安心 |
| Anchor Positioning | ✅ 125+ | ❌ 開発中 | ❌ 開発中 | 77% | ⚠️ PE推奨 |
| View Transitions | ✅ 111+ | ✅ 132+ | ✅ 18+ | 89.4% | ✅ 安心 |

**PE = プログレッシブエンハンスメント**：`@supports` で対応ブラウザのみに適用し、非対応ブラウザでは基本機能が動作するようにする。

---

## 実プロダクトでの採用事例

「スペック上は使えても、実際のプロダクトで使われているの？」と思うかもしれない。答えはYESだ。

### Tailwind CSS v4

2024年にリリースされたTailwind CSS v4は、**内部アーキテクチャを全面的にモダンCSS上に再構築**した。

- **`@layer`** でベース・コンポーネント・ユーティリティの優先順序を管理
- **CSS Nesting** を内部的に活用
- **`oklch()`** をデフォルトのカラーシステムに採用
- **`@property`** でカスタムプロパティの型定義を活用
- PostCSSへの依存を大幅に削減し、Lightning CSSベースに移行

Tailwindの作者Adam Wathan氏は「CSSの進化がTailwind v4を可能にした」と述べている。世界で最も使われているCSSフレームワークが、これらの機能に全面的に依存しているのだ。

### GitHub

GitHubのUIは、**Primer CSS**というデザインシステムで構築されている。

- **`@layer`** でデザインシステム内の詳細度管理を整理
- **`:has()`** でフォームの状態連動（例：エラーのあるフィールドのハイライト）
- **`color-mix()`** でダークモード/ライトモードのカラーバリエーション生成
- **View Transitions** でリポジトリ内のファイル遷移をスムーズ化

### Shopify

Shopifyのテーマシステムでも、モダンCSS機能の採用が進んでいる。

- **`@container`** でストアフロントのコンポーネント（商品カード、カート）を配置場所に依存しないレスポンシブに
- **Popover API** で商品フィルターやカート プレビューを実装
- **CSS Nesting** でテーマCSSの可読性を向上

これらの事例は、モダンCSSが「実験的な技術」ではなく、**実プロダクトで信頼されるツール**であることを示している。

---

## furetakoto.dev での実践案

このブログ自体で、モダンCSSをどう活かせるかを考えてみる。

### すぐに導入できるもの

**1. `@layer` でCSS設計を整理**

```css
@layer reset, tokens, base, components, utilities;

@layer tokens {
  :root {
    --hue: 260;
    --primary:    oklch(0.55 0.20 var(--hue));
    --surface:    oklch(0.98 0.01 var(--hue));
    --text:       oklch(0.15 0.02 var(--hue));
  }
}
```

**2. `oklch()` でカラーパレットを再構築**

HSLベースのカラー定義を `oklch()` に移行し、ダーク/ライトモードで一貫したカラーシステムを構築する。

**3. CSS Nesting でプリプロセッサを外す**

Sassのネスト記法のためだけにビルドプロセスを通しているなら、ネイティブネストへの移行で**ビルドステップを一つ減らせる**。

### 差別化に使えるもの

**4. View Transitions でページ遷移をリッチに**

ブログの一覧→記事詳細で、サムネイルが拡大しながらヒーロー画像になる遷移。これだけで体験が大幅に向上する。

**5. Scroll-driven Animations で読了プログレスバー**

```css
.reading-progress {
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 3px;
  background: var(--primary);
  transform-origin: left;
  animation: progress linear;
  animation-timeline: scroll();
  z-index: 1000;
}

@keyframes progress {
  from { transform: scaleX(0); }
  to   { transform: scaleX(1); }
}
```

JSゼロ、パフォーマンス影響ゼロの読了プログレスバーが手に入る。

**6. `:has()` でインタラクティブなコードブロック**

```css
/* コードブロックにホバーしたらコピーボタンを表示 */
.code-block:has(:hover) .copy-btn {
  opacity: 1;
}

/* 目次の現在セクションをハイライト */
.toc:has(a:target) {
  /* ... */
}
```

---

## まとめ — float も clearfix も autoprefixer ももう要らない

2026年のCSSは、もはや「スタイルシート」という名前が矮小化している。

| かつての課題 | 旧来の解決策 | 2026年のCSS |
|---|---|---|
| コンポーネント単位のレスポンシブ | JS + ResizeObserver | `@container` |
| 親要素の条件スタイリング | JS でクラス操作 | `:has()` |
| ネスト記法 | Sass / Less | CSS Nesting |
| 詳細度の管理 | BEM + !important | `@layer` |
| 知覚均一なカラー | カラーツール + 手動調整 | `oklch()` + `color-mix()` |
| display:none からのアニメ | Framer Motion 等 | `@starting-style` |
| スクロール連動 | GSAP / Intersection Observer | Scroll-driven Anim. |
| ポップオーバー / ツールチップ | Floating UI / Popper.js | Popover API + Anchor |
| ページ遷移アニメーション | SPA + ルーター | View Transitions |

これらの機能の多くは**ブラウザ対応率90%超**で、今日からプロダクションで使える。

もちろん、すべてを一度に導入する必要はない。まずは `@layer` と CSS Nesting から始めて、CSS設計をモダナイズする。次に `oklch()` でカラーシステムを整理する。そして `:has()` と `@container` でコンポーネントの表現力を高める。段階的に移行すれば、リスクを最小限に抑えながらモダンCSSの恩恵を受けられる。

**CSSは2015年じゃない。**

float でレイアウトを組み、clearfix で浮動を解除し、autoprefixer でベンダープレフィックスを付与し、Sass でネストを書き、BEM で命名規則を強制し、JavaScript でスクロールアニメーションを制御していた時代は終わった。

2026年のCSSは、単体で十分に強力だ。あなたのCSSもアップデートしよう。
